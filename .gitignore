# Pairing QR files contain the local backend access token.
ceviz-*-pairing.png

# Local credentials and generated artifacts.
.auth-token
.venv/
# Local code snapshots, update locks and recovery receipts (never user state).
.ceviz-updates/
__pycache__/
*.py[cod]
backend.log
build/
DerivedData/
*.xcodeproj/
*.xcarchive
*.ipa
.DS_Store
